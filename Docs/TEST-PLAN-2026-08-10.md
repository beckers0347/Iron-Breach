# Iron Breach — Tomorrow's Test Plan (Aug 10)

**Start here:** if you didn't run `zmenu.bat` yesterday, just run **`zmorning.bat`** — it covers everything (build + menu assets + push). If you DID run zmenu, still run zmorning: it builds the overnight code and skips whatever already exists. Wait for the console to close, then open the editor or repackage.

Repackage for the full experience: the overnight work is exactly the stuff PIE hides (Escape, packaged input). To repackage, tell Claude — or run the same BuildCookRun as yesterday.

## What got built overnight

1. **Escape menu everywhere** — Escape (or gamepad Start) opens the System screen: Resume / Main Menu / Quit. Works on foot, in the mech, while dead. The screen builds its own buttons in C++, so it can never ship empty again.
2. **E exits the mech** — both seats, raw key, no setup.
3. **The demo is now a mission** — deploy into Patrol ("SWEEP THE ZONE"), kaiju spawns → "!! KAIJU EMERGENCE DETECTED !!" banner (amber), then kill orders, kill it → "ZONE SECURED" (green). Objective banner top-center, automatic.
4. **Weapon wells on 1/2/3** — Primary/Special/Heavy switching, server-validated (empty wells refuse the switch instead of giving you a mystery rifle).
5. **Loot toasts** — pick up a drop → "+ Kaiju Chitin x3" bottom-right, tinted by rarity.
6. **Menus content pass (scripted)** — I opens Inventory (starter Service Rifle + Kaiju Chitin on the grid), L Ledger, M Map, Q/E cycles, Tab also Inventory. Starter rifle is wired to the real rifle data — equipping it IS the loot→gun seam.
7. **Mech cannon fights fair** — it now goes through the armor/organ phases instead of bypassing them.

## The test script (~20 min, in order)

**A. PIE solo:**
1. Boot screen → menu → SOLO → in level. Objective banner reads "SWEEP THE ZONE".
2. Press **I** → inventory grid shows Service Rifle + Kaiju Chitin. Click the rifle in the grid → it equips (well fills). **Q/E** cycles screens. **Escape** closes.
3. Press **Escape** in-game → System screen (three buttons) → Resume works.
4. Kaiju spawns → amber EMERGENCE banner → then kill orders.
5. Board the mech (X) → **press Escape inside the mech** → menu works → Resume → **press E** → you dismount beside the hull.
6. Kill the kaiju (organs → Exposed → dead) → **ZONE SECURED** in green. XP pops.
7. Walk over any loot drops → toast lines bottom-right.
8. Press **1/2/3** — 2 and 3 should do nothing (empty wells refuse); 1 stays primary.
9. Escape → Main Menu → lands on the menu cleanly. Escape → Quit works.

**B. Packaged (repeat A's steps 3, 5, 9 minimum)** — Escape/E/quit are the things PIE fakes.

**C. Two-player listen (when Shane's on):** client sees the same mission banners; both see EMERGENCE at the same moment; client Escape→Leave returns them to menu while host keeps playing.

## Known rough edges (expected, not bugs)
- Inventory/Ledger/Map screens are UNSTYLED — functional grids on transparent backgrounds. Shane's §5 pass makes them pretty.
- Map screen is empty until a zone capture + DA_Map exists (§7).
- If the menus don't open on I/L/M/Escape: the python asset step probably didn't run — check `Saved/ib_morning_report.txt` for `IBPY:` lines and send them to Claude.
- Gunner seat two-player is still the old always-driver behavior — retry is queued separately, deliberately not stacked on tonight.
